info:
  name: local sse
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:18080/sse
  headers:
    - name: Accept
      value: text/event-stream
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
